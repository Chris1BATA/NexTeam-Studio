# NexTeam Studio

NexTeam Studio is a separate standalone project for the AI agent workflow system.

It is intentionally split out from the Aquatrace app so the agent project can evolve independently as its own workspace.

The project currently includes:
- agent documentation
- helper scripts
- a standalone browser demo
"# NexTeam-Studio" 
